# Database Configuration (for Docker Compose)
DATABASE_URL="postgresql://user:password@localhost:5432/marketing_automation?schema=public"
REDIS_HOST=localhost
REDIS_PORT=6379

# Copy this file to .env and fill in the values
# For API-specific configuration, see apps/api/.env.example
# For Dashboard-specific configuration, see apps/dashboard/.env.local.example
